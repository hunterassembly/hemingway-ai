{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "predev": "cp ../dist/client/overlay.iife.js public/hemingway-client.js || echo 'Build hemingway first: npm run build'",
    "prebuild": "cp ../dist/client/overlay.iife.js public/hemingway-client.js || echo 'Build hemingway first: npm run build'",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/vercel": "^9.0.4",
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
